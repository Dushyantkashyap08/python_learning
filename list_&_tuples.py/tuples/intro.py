#tuples : collection of members just like lists but immutable

a = () #empty tuple
b = (1,) #tuple with only one element needs a comma
c = (1,2,3) #multiple elements
d = (1)

print(type(b)) #tuple type
print(type(d)) #int type as python interpreter will consider it as a single value
